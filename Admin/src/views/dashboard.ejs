<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
</head>
<body>
  <h1>Dashboard</h1>
  <a href="/news/add">Add News</a>
  <a href="/logout">Logout</a>
  <ul>
    <% news.forEach(item => { %>
      <li>
        <h2><%= item.title %></h2>
        <p><%= item.content %></p>
        <a href="/news/edit/<%= item._id %>">Edit</a>
        <form action="/news/delete/<%= item._id %>" method="post">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>
